# QRowg — dernière grande revue

*4 septembre 2026 · PC et téléphone · chaque point est mesuré ou lu dans le code, avec sa référence.*

## Point d'étape — 14 septembre

*Relu point par point contre le code, pas contre la mémoire.*

| | Fait | Reste |
|---|---|---|
| **P0** (15) | **15** | l'identité de l'éditeur sur `/legal` — SIRET, raison sociale, adresse : **toi** (obligation LCEN) |
| **P1** (44) | **44** | — |
| **P2** (21) | non audité | à reprendre |

**P0 — vérifié dans le code** : `/legal` lit `lib/editeur.ts` (les crochets ont
disparu, les valeurs restent à fournir) · pause/archiver/supprimer branchés ·
doublon « Supprimer mon compte » retiré du Profil · `verifyDomain` appelé, donc
rattachement Vercel réel · open redirect fermé (`resolve` joint sur le
propriétaire vérifié) · résiliation Stripe avant suppression de compte, avec
refus si Stripe échoue · double souscription bloquée par `stripe_customer_id` ·
`/api/emails/new-lead` répond 410 · quotas inventés retirés de la FAQ, de
Contact et du générateur · « Remboursement 14 jours » retiré (les CGU disaient
l'inverse) · politique de confidentialité réécrite.

**P1 — le dernier était P1-20**, corrigé par le lot v116 : le guide public
répondait « Peut-on suivre les scans d'un QR code gratuit ? **Non.** » alors que
`plans.ts` donne un QR modifiable au plan Gratuit — et qu'une **autre réponse du
même fichier** disait déjà l'inverse, elle, en lisant la grille. Les 43 autres
sont en place : 58 migrations dont six datées des 4 et 5 septembre pour la
sécurité et la base, `quotaApi`, `linkPassword` borné et limité en fréquence,
`ui-demo` en 404 hors harness, index utilisateur posés, polices réellement
servies.

**Ce qui n'est dans aucune de ces listes** : les lots v99 → v116. Ce sont des
défauts trouvés en mesurant, pas des points de la revue — dont un plus grave que
n'importe quel P0 : une note d'avis à −1 en base faisait **lever le rendu de la
page publique**, et le client qui venait de scanner voyait une page d'erreur au
lieu du menu (lot v113).

---

## État des lieux

- **2 753** — tests verts, 162 fichiers
- **0** — erreur TypeScript, 0 erreur de build
- **0** — vulnérabilité connue sur 183 dépendances
- **20 × 4** — modèles × largeurs (360, 390, 1024, 1440) : 0 débordement, 0 tap détourné
- **39** — thèmes, 0 paire de couleurs sous 4,5:1
- **14** — familles de polices, toutes servies
- **537 ko / 1,7 s** — page publiée avec photos, 4G bridée, premier affichage
- **< 1 s** — premier affichage de toutes les pages sur PC (ADSL 10 Mb/s)
- **1 / 7 j** — groupe d'erreurs en production (webhook Stripe, déjà corrigé, à déployer)

**80 points** : 15 bloquants ou trompeurs (P0), 44 importants (P1), 21 de finition (P2). Effort : S < 1 h · M une demi-journée · L 1 à 2 jours. « moi » = code, « toi » = compte, configuration ou décision.

## P0 — Bloquant ou trompeur

*À corriger avant toute campagne d'acquisition : un parcours cassé, une promesse fausse, une faille exploitable.*

### P0-1 · La page /legal affiche des crochets à la place de l'éditeur

**Mentions légales** · PC · Tél · effort S · toi

Preuve — src/app/legal/page.tsx:20-24 — « [ Nom société ] », « [ Numéro SIRET ] », « [ Adresse complète ] ». Page indexée et liée depuis tous les pieds de page.

Correctif — Renseigner l'identité réelle (obligation LCEN). Je pose les champs, tu fournis les valeurs.

### P0-2 · « Mettre en pause », « Archiver », « Supprimer » ne font rien

**QR de mes pages** · PC · Tél · effort M · moi

Preuve — QRStudio.tsx:558, :1134, :2660 — le menu « ⋯ » pose un état confirmAction que rien ne rend. La modale de suppression (:2510) est inatteignable ; deleteQR/archiveQR sont du code mort.

Correctif — Rendre la modale pilotée par confirmAction et brancher les trois actions.

### P0-3 · « Supprimer mon compte » n'a pas de onClick

**Profil › Sécurité** · PC · Tél · effort S · moi

Preuve — profile/page.tsx:2216-2219 — on tape SUPPRIMER, le bouton s'active, rien ne se passe. La vraie suppression vit dans Paramètres.

Correctif — Retirer ce doublon du Profil et renvoyer vers Paramètres (une seule maison pour la sécurité du compte).

### P0-4 · Un domaine « actif » n'est jamais rattaché à Vercel

**Domaines** · PC · Tél · effort M · moi

Preuve — DomainsPage.tsx:86-98 — verifyDomain (qui appelle addToVercel) n'est jamais invoquée ; onVerified marque « actif » côté client seulement. Le visiteur de mondomaine.fr obtient une erreur.

Correctif — Appeler verifyDomain dans onVerified, ou déclencher l'ajout Vercel depuis /api/domains/check.

### P0-5 · « Vérifier DNS » n'affiche rien si la ligne est repliée

**Domaines** · PC · Tél · effort S · moi

Preuve — DomainsPage.tsx:281-286 / :300-312 — le vérificateur est rendu dans l'accordéon fermé.

Correctif — Ouvrir la ligne quand on lance la vérification.

### P0-6 · Détournement du trafic d'un domaine client + open redirect

**Sécurité · domaines** · — · effort M · moi

Preuve — Policies de domain_redirects / domain_routes (migrations 007-009) : seul auth.uid() = user_id est vérifié, pas la propriété du domaine. resolve/route.ts:51-72 résout sans filtre. Un compte gratuit peut rediriger victim.com vers evil ; /api/domains/resolve?domain=… est un open redirect.

Correctif — Trigger exigeant un domain_verifications vérifié du même compte ; verrouiller verified/is_primary hors service role ; joindre sur le propriétaire dans resolve.

### P0-7 · Supprimer son compte n'annule pas l'abonnement Stripe

**Sécurité · compte** · — · effort S · moi

Preuve — api/account/delete/route.ts:35-42 — deleteUser sans stripe.subscriptions.cancel : le client continue d'être prélevé.

Correctif — Annuler les abonnements actifs avant la suppression ; refuser si Stripe échoue.

### P0-8 · Un abonné peut souscrire deux fois

**Sécurité · paiement** · — · effort S · moi

Preuve — api/stripe/checkout/route.ts:49-60 — pas de customer, pas de contrôle d'abonnement actif ; l'upsert du webhook écrase la ligne, le premier abonnement reste facturé et invisible.

Correctif — Passer stripe_customer_id, rediriger vers le portail si une souscription existe.

### P0-9 · /api/emails/new-lead est un relais d'e-mails ouvert

**Sécurité · e-mails** · — · effort S · moi

Preuve — new-lead/route.ts:14-19, 46-70 — anonyme, non lié à un lead réel, contenu libre, envoi depuis @qrowg.com au propriétaire de n'importe quelle page.

Correctif — Déclencher l'e-mail depuis /api/leads après l'insert, supprimer la route publique.

### P0-10 · La FAQ d'accueil annonce des quotas qui n'existent pas — et les injecte dans le JSON-LD

**Promesses** · PC · Tél · effort S · moi

Preuve — HomeClient.tsx:2973 « 3 pages, 200 vues par mois » (plans.ts : 1 page, vues illimitées) ; :2980 promet une alerte e-mail à 80 % d'un quota de vues inexistant.

Correctif — Réécrire depuis PLANS.free ; supprimer la question sur le quota de vues ; étendre promessesPlan.test.ts.

### P0-11 · La page Contact affiche l'ancienne grille tarifaire

**Promesses** · PC · Tél · effort S · moi

Preuve — contact/page.tsx:73 « Le plan Free donne accès à 3 pages, 200 vues/mois, 1 QR statique et 2 QR dynamiques/mois » — quatre chiffres faux, un nom de plan qui n'existe plus.

Correctif — Réécrire depuis PLANS.free.

### P0-12 · « Remboursement 14 jours » promis sur /upgrade, refusé dans les CGU

**Promesses** · PC · Tél · effort S · toi

Preuve — upgrade/page.tsx:290 vs terms/page.tsx:59 « Aucun remboursement prorata ». Aucune logique de remboursement dans le code.

Correctif — Retirer la mention, ou l'inscrire dans les CGU avec sa procédure. Ta décision.

**Tranché le 6 septembre : mention retirée.** Les trois arguments restants de la
grille — paiement Stripe, annulation à tout moment, sans engagement — sont, eux,
tenus par le code. Un test interdit désormais à toute page publique de promettre un
remboursement tant que les CGU n'en décrivent pas la procédure ; il s'ouvrira de
lui-même le jour où elles la décriront.

### P0-13 · Le générateur exige un « compte gratuit » qu'il n'exige pas ; « Gratuit 30 j (2/mois) » ressuscité

**Promesses** · PC · Tél · effort S · moi

Preuve — generateur-qr-code/page.tsx:16,24,29 vs GeneratorClient.tsx:326 « aucun compte requis » ; GeneratorClient.tsx:305 affiche l'essai que plans.ts interdit.

Correctif — « Gratuit, sans compte, sans filigrane » partout ; lire dynLimit() pour la ligne QR modifiable.

### P0-14 · La politique de confidentialité ne décrit pas ce que le code enregistre

**Confidentialité** · PC · Tél · effort M · moi

Preuve — privacy/page.tsx:108-110 « aucune donnée identifiable, IP non conservée » ; q/[code]/route.ts:368-395 stocke ville, OS, navigateur, referrer, empreinte IP hachée ; track/route.ts:86-116 stocke session, coordonnées de tap, durées. Sous-traitants manquants : Vercel (hébergement + analytics), Resend.

Correctif — Lister les champs réels, leur finalité et la durée de conservation (eventRetention.ts) ; ajouter les sous-traitants avec leur localisation.

### P0-15 · Sept lots livrés ne sont pas encore en production

**Déploiement** · — · effort S · toi

Preuve — page-publiee, contraste, texte-coupe, photos, photos-2, polices, creer-un-qr — plus le correctif du webhook Stripe « Invalid time value », seul groupe d'erreurs des 7 derniers jours.

Correctif — Passer les scripts dans l'ordre, puis git push.

**Mis à jour le 6 septembre.** Il n'y a plus de scripts à passer dans l'ordre : un
seul lot cumulatif remplace tous les précédents. Il repart du dernier commit
réellement déployé (`c23d778`) et remet tout ce qui manque depuis — P1, P2, les
découpes, et les correctifs du jour. Deux déploiements ont échoué avant qu'on
comprenne la cause : mes lots supposaient appliqués des scripts qui ne l'étaient
pas. Règle retenue : **chaque lot repart du dernier commit vert sur Vercel**, jamais
de ce qu'on suppose appliqué en local.

## P1 — Important

*Frustrant, incohérent ou risqué. Dans l'ordre où je les prendrais.*

### P1-1 · La barre latérale PC s'affiche puis disparaît à chaque chargement sur téléphone

**Tableau de bord** · Tél · effort M · moi

Preuve — Mesuré : NAV 239×686 px visible à 391 ms, réduite à 0 à 661 ms. isMobile démarre à false (DashboardShell.tsx:221) ; localStorage lu dans l'initialiseur (:204-209) → mismatch d'hydratation. Même famille dans BuilderV4:481, QRStudio:600, PrintStudioClient:184.

Correctif — Initialiser à partir d'un cookie lu côté serveur, ou ne rendre la barre qu'après montage.

### P1-2 · « ← QRowg » d'un visiteur sans compte mène à la page de connexion

**Éditeur (invité)** · PC · Tél · effort S · moi

Preuve — BuilderV4.tsx:1395 → /dashboard → redirect(/auth/login). L'essai sans compte se termine sur un mur.

Correctif — Pour un invité, pointer vers /dashboard/templates.

### P1-3 · Taper un bloc n'ouvre pas ses réglages

**Éditeur** · Tél · effort S · moi

Preuve — BuilderV4.tsx:1052-1057 — la sélection ne bascule pas sur l'onglet Réglages ; l'état vide dit « Clique sur un bloc dans le canvas » alors que le canvas est sur un autre onglet.

Correctif — Basculer sur l'onglet réglages à la sélection ; bouton « Voir la page » dans l'état vide.

### P1-4 · La fenêtre « Publier » n'est pas défilable et se coupe en bas

**Éditeur** · PC · Tél · effort S · moi

Preuve — BuilderV4.tsx:1555 — position:absolute, ~900 px de contenu, sans maxHeight ni overflow, dans un parent overflow:hidden. Sur 700-768 px de haut, les supports d'impression sont inatteignables. Pas de rôle dialog, pas d'Échap.

Correctif — Basculer sur le primitive Modal (défilement, focus, Échap).

### P1-5 · « Générer ma page avec l'IA » est mis en avant alors que la fonction est désactivée

**Éditeur** · PC · Tél · effort S · moi

Preuve — BuilderV4.tsx:2919-2947 + api/generate-page:25-30 — sans ANTHROPIC_API_KEY on rédige, on attend, puis « arrive très bientôt ».

Correctif — Ne pas monter le bloc quand la fonction est désactivée ; rendre l'upsell cliquable.

### P1-6 · « Plan pro requis » est un toast sans issue

**Modèles** · PC · Tél · effort S · moi

Preuve — templates/page.tsx:722, :727 — rouge, puis rien : ni lien vers les offres, ni explication.

Correctif — Toast avec action « Voir les offres » → /upgrade?reason=template.

### P1-7 · Si le réseau tombe, le bouton reste en chargement pour toujours

**Modèles · Domaines · Redirections** · Tél · effort S · moi

Preuve — templates/page.tsx:870-878, TemplateWizardModal.tsx:144-152, DomainsPage.tsx:71-84, RedirectsPanel.tsx:82-102 — fetch sans try/finally, submitting jamais remis à false.

Correctif — try/finally partout + « Connexion impossible. Réessayez. » comme le fait déjà l'onboarding.

### P1-8 · Des messages Supabase bruts, en anglais, montrés au commerçant

**Partout** · PC · Tél · effort S · moi

Preuve — DashboardClient.tsx:120,134 ; settings/page.tsx:106 ; profile/page.tsx:601 ; builder/publish.ts:31 ; team/page.tsx:52-118 (« Failed to fetch » affiché tel quel).

Correctif — Réutiliser builderErrors.safeErrorMessage partout, avec un repli explicite.

### P1-9 · « Mensuel / Annuel −20 % » est un faux interrupteur

**Profil › Abonnement** · PC · Tél · effort S · moi

Preuve — profile/page.tsx:2288-2296 — état local seulement, rien n'est envoyé à Stripe ; on croit passer en annuel et on est facturé au mois. « Gérer la facturation » sans état de chargement (double clic → deux portails).

Correctif — Retirer le toggle ou le lier au portail ; loading sur le bouton.

### P1-10 · Suppressions immédiates, sans confirmation, sur des cibles de 26-28 px

**Profil · Domaines · Redirections · Objectifs** · Tél · effort S · moi

Preuve — profile/page.tsx:3028 (26×26), DomainsPage.tsx:288 (28×28, sans aria-label), RedirectsPanel.tsx:323, GoalsDashboard.tsx:246,539. Le useConfirm maison existe et est utilisé ailleurs.

Correctif — await confirm({ danger }) + cibles ≥ 40 px + aria-label.

### P1-11 · Suppressions « optimistes » qui masquent l'échec

**QR · Messages · Domaines · Redirections · Objectifs** · PC · Tél · effort S · moi

Preuve — QRStudio.tsx:686-704, :1118 ; LeadsClient.tsx:68-80 ; DomainsPage.tsx:111 ; RedirectsPanel.tsx:116 ; GoalsDashboard.tsx:246 — la réponse n'est pas lue ; l'élément disparaît et revient au rechargement.

Correctif — Vérifier error/res.ok, restaurer l'élément, toast d'échec (comme DashboardClient.deletePage).

### P1-12 · « Préférences enregistrées ! » même si l'écriture a échoué

**Paramètres** · PC · Tél · effort S · moi

Preuve — settings/page.tsx:122-140 — update sans lecture de error.

Correctif — Tester error, afficher l'échec.

### P1-13 · Un import qui échoue ne dit rien

**Médias** · Tél · effort S · moi

Preuve — assets/page.tsx:59-66 — uploadImage renvoie null en silence ; aucune validation de taille avant envoi.

Correctif — Valider type/taille avant, afficher lastError après.

### P1-14 · Mauvais message pour un visiteur sans compte (closure périmée)

**Éditeur · Import d'image** · PC · Tél · effort S · moi

Preuve — ImageUpload.tsx:78,83 ; FileUpload.tsx:39,45 — lastError lu après await depuis le rendu précédent : « Erreur upload — réessaie » au lieu de « Créez un compte ». Le bon message arrive au 2ᵉ essai.

Correctif — Faire renvoyer la raison par uploadImage ({ url, error }).

### P1-15 · Messages, Médias, Équipe, Domaines, Redirections, Paramètres n'ont aucune entrée sur téléphone

**Navigation** · Tél · effort M · moi

Preuve — MobileNav.tsx:66-72 — cinq onglets ; le badge « non lus » calculé dans le shell n'a pas de destination mobile.

Correctif — Un onglet « Plus » listant les sections manquantes, avec le badge Messages.

### P1-16 · « ImageIcon trop lourde (max 5 Mo) »

**Profil** · PC · Tél · effort S · moi

Preuve — profile/page.tsx:693 — un renommage d'import appliqué au texte lu par l'utilisateur.

Correctif — « Image trop lourde (max 5 Mo) ».

### P1-17 · Le domaine personnalisé est une redirection vers qrowg.com, pas « ton image »

**Promesses** · PC · Tél · effort L · toi

Preuve — domains/resolve/route.ts:123,138 et subdomain/resolve:64 font redirect(qrowg.com/slug) ; features/page.tsx:469 promet « Ton image, pas la nôtre » ; landingJsonLd.ts:77 « marque blanche ». Aucune implémentation distincte de la marque blanche (seul removeBranding, déjà en Établissement).

Correctif — Soit passer au rewrite (le visiteur reste sur son domaine), soit reformuler « redirection depuis votre domaine » et retirer « marque blanche ». Ta décision.

### P1-18 · Statistiques détaillées annoncées « dans tous les plans »

**Promesses** · PC · Tél · effort S · moi

Preuve — features/page.tsx:395-403 vs plans.ts:83 dynStatsDetaillees:false en Gratuit.

Correctif — « Statistiques de base incluses, détail par appareil dès Établissement ».

### P1-19 · Les plans s'appellent Pro/Business ici, Établissement/Multi-sites là — sur la même page

**Promesses** · PC · Tél · effort S · moi

Preuve — HomeClient.tsx:693,708-709,2974,2976 ; upgrade/page.tsx:248 ; features/page.tsx:469-471 ; examples/page.tsx:14.

Correctif — Dériver tous les libellés de PLANS[id].label ; test.

### P1-20 · Le guide dit que le QR dynamique « nécessite un abonnement »

**Promesses** · PC · Tél · effort S · moi

Preuve — guides.ts:76 et :154 « Peut-on suivre les scans d'un QR gratuit ? Non. » — plans.ts inclut 1 QR modifiable en Gratuit.

Correctif — « Inclus dès le plan gratuit (1 QR modifiable) ».

### P1-21 · « Tous les templates inclus » sur Exemples, « 5 styles visuels » inventés sur Fonctionnalités

**Promesses** · PC · Tél · effort S · moi

Preuve — examples/page.tsx:373 vs plans.ts:225 (27 gratuits) ; features/page.tsx:374 « Classic, Gold, Neon, Sunset, Business » vs 8 formes et 107 préréglages réels.

Correctif — Décrire la vraie offre.

### P1-22 · Quotas API « 1 000 / 10 000 appels par mois » non appliqués

**Promesses** · PC · Tél · effort M · moi

Preuve — plans.ts:124,150,230 ; seul contrôle : 120 requêtes/min identiques pour les deux plans (api/v1/pages/route.ts:12).

Correctif — Implémenter le compteur mensuel, ou annoncer « 120 requêtes/min ».

### P1-23 · Résiliation contradictoire

**CGU** · PC · Tél · effort S · moi

Preuve — terms/page.tsx:59-60 « passe en Free, données conservées 30 jours avant suppression » — aucun cron de purge, « Free » n'existe pas.

Correctif — « Votre compte passe au plan Gratuit ; vos pages restent en ligne dans les limites de ce plan ».

### P1-24 · Le guide RGPD et la page Sécurité minimisent ou inventent

**Guide RGPD · Sécurité** · PC · Tél · effort S · toi

Preuve — guides.ts:322,330 « ni ville ni… » (faux, cf. P0) ; security/page.tsx:47 « sauvegardes automatiques » et :32 « migrations de durcissement » invérifiables ; trois descriptions d'hébergement différentes (security:46, legal:37, privacy:131).

Correctif — Aligner sur la liste réelle ; une seule phrase d'hébergement avec la région exacte — à me confirmer.

### P1-25 · Trois statuts Stripe absents de l'enum → 500 et plan payant conservé

**Sécurité · Stripe** · — · effort S · moi

Preuve — initial_schema.sql:16 (active|trialing|past_due|canceled|paused) ; webhook écrit sub.status tel quel → incomplete, incomplete_expired, unpaid font échouer l'upsert.

Correctif — Élargir l'enum ; rétrograder plan sur unpaid / incomplete_expired.

### P1-26 · Mot de passe de lien en GET, sans anti-brute-force

**Sécurité · lien protégé** · — · effort S · moi

Preuve — q/[code]/route.ts:158 form method=get (?pw= dans l'historique et les logs) ; :284 vérification avant tout rate-limit.

Correctif — POST + rateLimit(pw:code:ip) avant vérification.

### P1-27 · scryptSync sur un mot de passe non borné bloque le thread

**Sécurité · QR instantané** · — · effort S · moi

Preuve — qr-instant/route.ts:170-173.

Correctif — Refuser au-delà de 128 caractères.

### P1-28 · log_activity et increment_redirect_hit exécutables par tout client avec un user_id libre

**Sécurité · base** · — · effort S · moi

Preuve — 20260727130000:102-118 — SECURITY DEFINER, EXECUTE accordé à public ; fausses entrées possibles dans la timeline d'un autre compte.

Correctif — revoke execute from public/anon/authenticated, ou forcer p_user_id := auth.uid().

### P1-29 · Policy team_members FOR ALL sans WITH CHECK

**Sécurité · équipe** · — · effort S · moi

Preuve — team_rls.sql:109-119 — un admin peut insérer n'importe quel user_id avec n'importe quel rôle, sans invitation ni plafond de sièges.

Correctif — with check limitant l'insert au service role.

### P1-30 · Les triggers de quota lisent profiles sous la RLS de l'appelant

**Sécurité · quotas** · — · effort S · moi

Preuve — quotas_plan.sql:92,178 — un éditeur d'équipe activant un QR du propriétaire lit v_plan NULL → limites free → refus à tort. À vérifier en conditions réelles.

Correctif — security definer set search_path = public.

### P1-31 · /api/subdomain annonce « disponible » tout nom déjà pris, puis 500

**Sécurité · sous-domaine** · — · effort S · moi

Preuve — subdomain/route.ts:41-53 — client session ne voit que sa propre ligne depuis la fermeture de profiles ; le POST retombe sur 23505.

Correctif — createAdminClient pour le test d'unicité, 23505 → 409 (username-libre fait déjà bien).

### P1-32 · set_primary non transactionnel ; page_id jamais renseigné

**Sécurité · domaines** · — · effort M · moi

Preuve — domains/route.ts:145-191 — is_primary effacé partout avant d'être reposé (0 ligne → plus de principal) ; :137 existing.id est un id de vérification, pas de page.

Correctif — Valider/normaliser, RPC transactionnelle, existing.page_id.

### P1-33 · Rapports hebdo envoyés à une adresse tierce arbitraire ; « Merci {name} » vers n'importe qui

**Sécurité · e-mails** · — · effort S · moi

Preuve — reports/subscribe/route.ts:15-49 accepte email du body sans vérification ; chaîne /api/leads + lead-confirmation.

Correctif — Ignorer l'email du body (profil uniquement) ; confirmation envoyée depuis /api/leads seulement si le propriétaire l'a activée.

### P1-34 · Inondation possible de page_events

**Sécurité · statistiques** · — · effort S · moi

Preuve — track/route.ts:107-116 — 300 lignes par appel × 60/min/IP sans limite par page.

Correctif — Limite dédiée par page + plafond ~50 lignes.

### P1-35 · Charges JSON non bornées persistées

**Sécurité · entrées** · — · effort M · moi

Preuve — templates/use:48-113 (blocs illimités), qr-instant:85, print-design:45, leads:34, qr-status:34 (pause_message sans type), goals:34-75 (page_id non vérifié).

Correctif — Bornes de taille, whitelist de champs, vérification de propriété.

### P1-36 · Sept points de robustesse serveur

**Sécurité · divers** · — · effort M · moi

Preuve — contact rate-limit en mémoire locale (:8-21) ; secret cron accepté en query string et comparé par includes (gardeCron.ts:22,53) ; /api/cron/etat lisible par tout utilisateur ; qr-duplicate en Math.random sans unicité, custom_domain conservé (:10-67) ; écritures non vérifiées (qr-label:17, qr-destination:135, pages/create:75) ; clés « constructor » sur objets littéraux (new-lead:46, checkout:30) ; value.trim() sans typeof (v1 destination:15).

Correctif — Une passe dédiée : Upstash partout, Authorization + timingSafeEqual, allow-list admin, uniqueShortCode, .select().length, Object.hasOwn, typeof.

### P1-37 · Index manquants sur qr_codes(user_id) et team_members(user_id)

**Sécurité · base** · — · effort S · moi

Preuve — countActiveQrs et le trigger quota_qr_codes comptent par utilisateur à chaque insert ; can_read_owner évalué par ligne dans toutes les policies d'équipe.

Correctif — Deux index.

### P1-38 · Contournement de la résolution custom-domain sur un chemin contenant un point

**Sécurité · middleware** · — · effort S · moi

Preuve — middleware.ts:19 pathname.includes('.') → client.com/foo.bar sert le contenu qrowg.com sous l'hôte du client.

Correctif — Tester une vraie extension (/\.[a-z0-9]{2,5}$/).

### P1-39 · Texte de lecture sous 12 px sur cinq pages

**Site public** · PC · Tél · effort S · moi

Preuve — Mesuré : accueil 10,5 px (« Menu · Réservation · Avis Google » ×6), /creer et galerie 11 px (descriptions de modèles ×8) et 9 px (« ✦ Catalogue produits + promo »), /upgrade 11 px (sous-titres de plans ×3), générateur 11,5 px, éditeur 10,5 px (descriptions de blocs).

Correctif — Plancher 12,5 px pour tout texte de lecture ; étendre lisibilite.test.ts au site public.

### P1-40 · Hiérarchie de titres cassée

**Site public · Éditeur** · PC · Tél · effort S · moi

Preuve — Mesuré : /examples, /creer et la galerie sautent h1 → h3 ; l'éditeur n'a aucun h1.

Correctif — h2 sur les groupes, h1 visuellement discret dans l'éditeur.

### P1-41 · Boutons-icônes et liens sans nom accessible

**Tableau de bord** · PC · Tél · effort S · moi

Preuve — Mesuré : bouton 26×26 sans nom sur toutes les pages du tableau de bord (replier la barre), quatre liens de la barre réduite sans texte ni aria-label ; DomainsPage:270-276, profile:3015-3034, BuilderV4:1663.

Correctif — aria-label systématique.

### P1-42 · /dashboard/ui-demo est public en production

**Tableau de bord** · — · effort S · moi

Preuve — Page de revue des primitives UI, sans garde ni authentification.

Correctif — La passer par harnessAutorise() ou la supprimer.

### P1-43 · Un appel 401 part à chaque ouverture pour un visiteur sans compte

**QR vers un lien (invité)** · PC · Tél · effort S · moi

Preuve — Mesuré dans la console ; un fetch protégé lancé sans session.

Correctif — Ne pas lancer l'appel en mode invité.

### P1-44 · « Fraunces » et « DM Sans » restent des alias vers Inter

**Décision** · PC · Tél · effort S · toi

Preuve — 279 endroits de l'interface les nomment, logo compris ; les dé-aliaser change la typographie de tout le produit. Les 12 autres familles sont désormais réelles.

Correctif — Ta décision : garder Inter partout (rien à faire), ou activer les vraies polices (S, puis contrôle visuel de l'app).

## P2 — Finition

*Ce qui sépare « propre » de « parfait ».*

### P2-1 · og:locale perdu sur toutes les sous-pages ; Article JSON-LD sans image ; fil d'Ariane qui répète la catégorie ; og:type profile pour un restaurant

**SEO technique** · — · effort S · moi

Preuve — features/layout.tsx:10 et 13 autres ; guides/[slug]/page.tsx:46-59 ; [slug]/page.tsx:65.

Correctif — Un helper ogFor(url, title, desc) ; image + dates par guide ; name: g.h1 ; type website par défaut.

### P2-2 · Descriptions hors fenêtre et titres génériques

**SEO technique** · — · effort S · moi

Preuve — outils (183 car.), testeur (182), taille (178), upgrade (108) ; titres « Contact », « Fonctionnalités », « Exemples de pages ».

Correctif — 120-160 caractères et un mot-clé par titre ; étendre seo.test.ts aux pages statiques.

### P2-3 · lastmod du sitemap déconnecté des dates réelles ; themeColor différent entre layout et manifest ; BOM en tête de deux fichiers

**SEO technique** · — · effort S · moi

Preuve — sitemap.ts:137 (2026-08-24) vs guides.ts:31 (08-11), security (08-12), légal (15 juin) ; layout.tsx:15 #C9A84C vs manifest.ts:234 #080808.

Correctif — Porter la date depuis chaque source ; aligner ; retirer les BOM.

### P2-4 · Description et JSON-LD basés sur le nom du titulaire du compte

**Page publiée** · — · effort S · moi

Preuve — [slug]/page.tsx:54-56 « Jean Dupont sur QRowg » en extrait Google d'une brasserie ; mainEntity Person pour toute page.

Correctif — page.title d'abord ; Organization/LocalBusiness sauf modèle personnel.

### P2-5 · Galerie sans texte alternatif

**Page publiée** · — · effort S · moi

Preuve — PublicPageClient.tsx:883, GalleryPublic — alt="" ; le bloc n'a aucun champ légende.

Correctif — Champ alt par image, ou reprendre c.title.

### P2-6 · Sur grand écran, la page est une colonne de 480 px au centre d'un fond vide

**Page publiée** · PC · effort L · toi

Preuve — Mesuré à 1440 px : lisible, sans défaut, mais deux tiers de l'écran inoccupés.

Correctif — Optionnel : disposition à deux colonnes au-delà de 1024 px (profil à gauche, contenu à droite). Décision de design.

### P2-7 · Tutoiement sur Fonctionnalités, Exemples, Contact ; vouvoiement partout ailleurs

**Contenu** · PC · Tél · effort S · moi

Preuve — features/page.tsx:300,344,417 ; examples/page.tsx:367 ; contact/page.tsx:77-115 (la méta de la même page vouvoie) ; profile:2212, BuilderV4:2543,2925, builderPanels:1866,1930.

Correctif — Vouvoiement partout.

### P2-8 · Anglicismes en contradiction avec le vocabulaire retenu

**Contenu** · PC · Tél · effort S · moi

Preuve — « builder », « template », « dashboard », « analytics », « drag & drop » sur features, examples, contact, pied de page (HomeClient:3889-3891), JSON-LD (layout.tsx:115), image OG (:297) ; barre latérale « Dashboard / Templates / Print Studio / Analytics » vs barre mobile « Mes pages / Stats ».

Correctif — « éditeur », « modèle », « tableau de bord », « statistiques », « atelier d'impression » — mêmes noms partout ; étendre nomsEtQuotas.test.ts.

### P2-9 · Accents manquants et jargon

**Contenu** · PC · Tél · effort S · moi

Preuve — profile/page.tsx (« Domaine supprime », « Gerer la facturation », « Cle API creee »…), templates/page.tsx:794, QRStudio:2603 ; « Slug public », « Username », « Webhooks », « SDK » montrés à un artisan ; « WiFi » / « Wi-Fi » mélangés ; « 24 h ouvrées » / « 24h ».

Correctif — Passe d'accentuation ; « Adresse de la page », « Identifiant » ; API dans Paramètres › Avancé ; « Wi-Fi » partout.

### P2-10 · Lien de pied de page vers une route privée ; verticales WiFi et restaurant/menu qui se cannibalisent

**Contenu** · PC · Tél · effort S · moi

Preuve — HomeClient.tsx:3890 /dashboard/builder/new (bloqué par robots) ; /qr-code/wifi CTA vers /creer au lieu du générateur Wi-Fi ; FAQ dupliquées.

Correctif — Pointer vers /creer ; CTA wifi → /generateur-qr-code-wifi ; différencier les FAQ.

### P2-11 · « Sessions actives » est une liste simulée ; Objectifs : échec silencieux ; champs sans label

**Profil** · PC · Tél · effort S · moi

Preuve — profile/page.tsx:307-316 ; GoalsDashboard.tsx:220-244 ; team/page.tsx:178-186, qr-link/page.tsx:468-514.

Correctif — Libeller « cet appareil » ; afficher d.error ; label ou aria-label.

### P2-12 · Quatre modales maison sans rôle ni gestion du focus ; window.confirm dans la coquille mobile

**Modales** · PC · Tél · effort M · moi

Preuve — TemplatePreviewModal.tsx:2633, nommage templates/page.tsx, popup Publier, sheet « ⋯ » BuilderV4:2830 ; MobileBuilderShell.tsx:188.

Correctif — Migrer vers components/ui/Modal ; passer confirm depuis BuilderV4.

### P2-13 · CSS global injecté qui touche tout le tableau de bord

**QR de mes pages** · PC · Tél · effort S · moi

Preuve — QRStudio.tsx:4212 — button:active { transform } et input:focus !important non scopés.

Correctif — Préfixer par .qr-grid.

### P2-14 · Commandes de la barre du haut trop petites au clic

**Éditeur** · PC · effort S · moi

Preuve — Mesuré à 1440 px : nom de la page 160×15, « Focus » 60×23, flèches 22×22.

Correctif — Hauteur 32 px minimum sur PC.

### P2-15 · Liens de navigation de 16-19 px de haut

**Site public** · PC · effort S · moi

Preuve — Mesuré : « Tarifs », « Connexion », « ← Retour » entre 15 et 19 px sur toutes les pages secondaires ; correct à la souris, sous le minimum WCAG 2.5.8 (24 px).

Correctif — min-height 24 px sur les liens d'en-tête PC (la règle mobile existe déjà).

### P2-16 · Coquille mobile redesign (flag OFF aujourd'hui) : Thème, Modèles, QR et nom de page inaccessibles

**Éditeur** · Tél · effort M · moi

Preuve — MobileBuilderShell.tsx monté par-dessus la topbar ; bloquant le jour où NEXT_PUBLIC_BUILDER_REDESIGN passe à ON.

Correctif — Onglet « Style » + nom éditable dans l'en-tête — avant toute activation du flag.

### P2-17 · Suppression d'un bloc : confirmée ici, pas là

**Éditeur** · PC · Tél · effort S · moi

Preuve — BlockSettingsPanel confirme ; BuilderV4:2744, :2825 et la barre mobile non.

Correctif — Une seule règle : toast « Bloc supprimé — Annuler ».

### P2-18 · Les 18 images des aperçus de l'éditeur sont encore des <img> bruts

**Éditeur** · PC · Tél · effort S · moi

Preuve — shared-renderer/blocks/*/Editor*.tsx — sans conséquence pour le visiteur, coûteux pour l'auteur sur téléphone.

Correctif — Même conversion que le rendu public.

### P2-19 · L'accueil embarque 471 ko de JavaScript

**Performance** · PC · Tél · effort M · moi

Preuve — Mesuré : HomeClient.tsx, 3 976 lignes, un seul composant client. LCP 0,9 s sur PC, 1,65 s sur téléphone 4G — acceptable, mais c'est la page la plus lourde du site.

Correctif — Découper en sections chargées à la demande (démo de l'éditeur, FAQ, tarifs).

### P2-20 · Cinq fichiers de plus de 3 000 lignes cachent les bugs P0

**Dette** · — · effort L · moi

Preuve — QRStudio 4 215, HomeClient 3 976, profile 3 283, PublicPageClient 3 186, blockDefs 3 050, BuilderV4 3 042. Les trois mécanismes de confirmation débranchés de QRStudio en sont le symptôme.

Correctif — Découper au fil des correctifs (QRListColumn, QRExportPanel ; profile/Identite, Securite, Abonnement ; PublishPopover, TemplatesModal).

### P2-21 · 801 « any », 20 eslint-disable, un test instable non identifié, le glisser-déposer de l'atelier d'impression jamais construit sur téléphone

**Dette** · — · effort L · moi

Preuve — Relevé statique ; 22 exécutions de la suite n'ont pas reproduit le test instable.

Correctif — Chantier de fond, au fil de l'eau.

## À faire par toi, hors code

1. **Passer les 7 scripts en attente dans l'ordre, puis git push** — Le correctif du webhook Stripe n'est pas déployé ; c'est le seul groupe d'erreurs des 7 derniers jours.
2. **Renseigner l'identité légale** — Nom, forme, SIRET, adresse, responsable de publication — pour /legal.
3. **Me confirmer la région d'hébergement** — Projet Supabase et région Vercel : une seule phrase, la même sur Sécurité, Mentions légales et Confidentialité.
4. **Décider : remboursement 14 jours** — Le retirer de /upgrade, ou l'inscrire dans les CGU avec sa procédure.
5. **Décider : domaine personnalisé** — Redirection (état actuel) ou vrai domaine (rewrite, un lot L). La promesse marketing dépend de la réponse.
6. **Décider : Fraunces et DM Sans** — Garder Inter dans l'interface (rien à faire), ou activer les vraies polices partout.
7. **Faire tourner les clés Stripe** — Précaution, pas urgence — l'historique public du dépôt ne contient pas de secret (vérifié).
8. **Activer la protection contre les mots de passe divulgués** — Supabase → Authentication. Signalé par l'advisor.
9. **Archiver les 12 anciens produits « QR Dynamique » Stripe et supprimer les 6 variables NEXT_PUBLIC_STRIPE_DYN_* sur Vercel** — Ménage.
10. **Coller le vrai SOCIAL_UPLOAD_TOKEN et réactiver les deux agents marketing**
11. **Tester customer.subscription.deleted en annulant immédiatement** — Jamais exercé ; sinon il se déclenchera seul le 1er octobre.
12. **Me dire qui est le premier client** — Restaurant, artisan, agence ? La bascule vers l'acquisition en dépend — le goulot est la demande, pas le code.

---
*Méthode : quatre revues de code en lecture seule (sécurité et routes serveur, expérience du tableau de bord et de l'éditeur, SEO et contenu, base statique), puis un balayage au navigateur de 25 routes × 2 largeurs (1440 et 390 px) et de la page publiée à 360, 390, 1024 et 1440 px avec photos, avec des instruments validés par injection de défauts connus. Ce qui n'a pas pu être mesuré est signalé « à vérifier ».*